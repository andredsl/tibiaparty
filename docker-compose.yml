version: '3.8'

services:
  # Aplicacao Spring Boot
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tibia-party-finder
    ports:
      - "8080:8080"
    environment:
      - SPRING_DATASOURCE_URL=jdbc:mysql://shuttle.proxy.rlwy.net:55402/railway?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&createDatabaseIfNotExist=true
      - SPRING_DATASOURCE_USERNAME=root
      - SPRING_DATASOURCE_PASSWORD=dEYvXpjxFrkXOfvrHvakBPgbwdgzkSrW
      - SPRING_DATASOURCE_DRIVER_CLASS_NAME=com.mysql.cj.jdbc.Driver
      - SPRING_JPA_HIBERNATE_DDL_AUTO=update
      - SPRING_JPA_DATABASE_PLATFORM=org.hibernate.dialect.MySQLDialect
      - SPRING_THYMELEAF_CACHE=true
      - TZ=America/Sao_Paulo
    restart: unless-stopped
