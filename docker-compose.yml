version: '3.8'

services:
  # Aplicacao Spring Boot
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tibia-party-finder
    ports:
      - "8080:8080"
    environment:
      - SPRING_DATASOURCE_URL=jdbc:mysql://shuttle.proxy.rlwy.net:55402/tibia_party_finder?useSSL=true&allowPublicKeyRetrieval=true&serverTimezone=UTC
      - SPRING_DATASOURCE_USERNAME=root
      - SPRING_DATASOURCE_PASSWORD=dEYvXpjxFrkXOfvrHvakBPgbwdgzkSrW
      - SPRING_JPA_HIBERNATE_DDL_AUTO=update
      - SPRING_THYMELEAF_CACHE=true
      - TZ=America/Sao_Paulo
    restart: unless-stopped
